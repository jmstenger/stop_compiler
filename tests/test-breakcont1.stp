/*
 * test-breakcont1.stp
 * =====================
 */

def main = ():Int {
	var i:Int;
	for (i = 0; i <10; i = i +1) {
		if (i==3){
			continue;
		}

		if (i==7){
			break ;
		}

		printf("%d",i);
	}

	return 0;
}
