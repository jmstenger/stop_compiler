/*
 * test-controlflow9.stp
 * =====================
 */

def main = ():Int {
	var i:Int;
	var j:Int;
	for (i = 0; i <5; i = i +1) {
		printf("%d", i);

		for (j=i; j>0 ; j=j-1){
			printf("%d",j);
		}
	}

	return 0;
}
